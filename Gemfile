source "https://rubygems.org"

# Pinned for reproducible builds locally and on GitHub Actions.
gem "jekyll", "~> 4.3"
gem "webrick"            # required for `jekyll serve` on Ruby 3+

group :jekyll_plugins do
  gem "jekyll-seo-tag",  "~> 2.8"
  gem "jekyll-sitemap",  "~> 1.4"
end
